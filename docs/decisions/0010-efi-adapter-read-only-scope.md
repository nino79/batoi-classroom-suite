# ADR-0010: EFI Adapter — Read-Only, Domain-Named Firmware Boot Configuration Integration

**Status:** Accepted

## Context

Host Inventory ([ADR-0008](0008-host-inventory-ports-and-adapters.md)) can report *whether* a machine is UEFI-capable and its Secure Boot state, but has no visibility into actual UEFI boot entries (`BootOrder`, `BootCurrent`, individual `Boot####` entries). Boot Manager's own architecture ([docs/architecture/boot-manager.md § Open Questions](../architecture/boot-manager.md#open-questions)) already flags UEFI NVRAM boot-entry management (`BM-003`) as unresolved, with reliability known to vary significantly by OEM firmware.

The Platform Layer ([ADR-0009](0009-platform-layer-command-runner.md)) already anticipated this exact adapter in [docs/PLATFORM_LAYER.md § How Future Adapters Use It](../PLATFORM_LAYER.md#how-future-adapters-use-it), naming `efibootmgr` as the first candidate. This ADR is the first adapter built on that foundation — the start of a broader **Host Discovery** effort (read-only inspection adapters), of which this is the first piece.

Building a read-only inspection capability first, before any UEFI boot-entry *write* capability is designed, lets BCS observe real boot-entry data across real classroom hardware before committing to how (or whether) BCS should ever change it — a deliberately cautious sequencing given how firmware-quirky NVRAM boot-entry management can be.

This ADR was reviewed and accepted with six amendments, incorporated directly into the Decision below rather than recorded as a later addendum, since nothing was implemented between proposal and acceptance (contrast [ADR-0008](0008-host-inventory-ports-and-adapters.md), whose amendment came after code already existed).

## Decision

1. **This adapter is permanently read-only.** It never modifies EFI variables, never changes boot order, and never installs rEFInd or any other boot loader/manager. The only `efibootmgr` flag it ever passes is `-v`. Full design in [docs/EFI_ADAPTER.md](../EFI_ADAPTER.md).
2. **The package is domain-named: `bcs.platform.adapters.efi`, not `.efibootmgr`.** It represents the EFI domain, not the current implementation technology; a future backend (`efivarfs`, `libefivar`, or anything else) could replace `efibootmgr` internally without a public rename. This is the first application of a new, project-wide rule — see point 4.
3. **A pure-function parser, completely independent of the execution layer.** `parse_firmware_boot_configuration(text: str) -> FirmwareBootConfiguration` accepts only a string, produces only immutable Pydantic models, never imports `CommandRunner` or `subprocess`, and never knows where its input text came from — it is designed to be extractable as a standalone library with no changes. `read_firmware_boot_configuration(runner: CommandRunner) -> FirmwareBootConfiguration` is a thin orchestration layer that calls it. This maximizes what's testable without mocking (see [docs/EFI_ADAPTER.md § Testing Strategy](../EFI_ADAPTER.md#testing-strategy)).
4. **A new project-wide architectural rule: domain-driven naming.** *"Packages, adapters, and Pydantic models shall be named after the business/domain concept, never after the implementation technology."* Recorded in [docs/standards/naming-conventions.md § Domain-Driven Naming](../standards/naming-conventions.md#domain-driven-naming) and [docs/standards/coding-standards.md § Domain-Driven Naming](../standards/coding-standards.md#domain-driven-naming), and cross-referenced from [ARCHITECTURE.md §5](../../ARCHITECTURE.md#5-cross-cutting-concerns). Applied here: `EfiBootConfiguration` → `FirmwareBootConfiguration` (see [docs/EFI_ADAPTER.md § Pydantic Models](../EFI_ADAPTER.md#pydantic-models) for the specific naming-collision rationale), `EfibootmgrError`/`EfibootmgrUnavailableError`/`EfibootmgrParseError` → `FirmwareBootError`/`FirmwareBootUnavailableError`/`FirmwareBootParseError`. `BootEntry` required no change — it was already domain-named.
5. **The locale policy is promoted from an EFI-specific detail to a Platform Layer-wide rule**: every adapter executing a Linux command via `CommandRunner` MUST force `LANG=C`/`LC_ALL=C`, building a full `os.environ` copy plus the two overrides (`CommandRunner`'s `env` replaces rather than merges — [docs/PLATFORM_LAYER.md § CommandRunner API](../PLATFORM_LAYER.md#commandrunner-api)). Recorded in [docs/PLATFORM_LAYER.md § Locale Policy](../PLATFORM_LAYER.md#locale-policy); this adapter is its first conforming example, not its origin.
6. **Command execution metadata does not belong on `FirmwareBootConfiguration`.** Evaluated and rejected on domain-purity grounds (the model represents a fact about the machine, not about the act of probing it) and because the pure parser that builds it structurally has no access to execution facts. A reusable `CommandMetadata`/envelope concept for future adapters is documented as a deferred idea in [docs/EFI_ADAPTER.md § Command Metadata](../EFI_ADAPTER.md#command-metadata), not concluded to be architecturally necessary yet, and not designed further here.
7. **Internally organized as a small subpackage** (`bcs/platform/adapters/efi/{models,parser,adapter,errors}.py`) rather than the single flat file [docs/PLATFORM_LAYER.md § Package Structure](../PLATFORM_LAYER.md#package-structure) originally sketched for adapters in general. The public import surface (`bcs.platform.adapters.efi`) is unchanged either way.

### Alternatives Considered

- **Keep the package named `efibootmgr`**, since that is the concrete tool it wraps today. Rejected: it ties a public, durable name to a specific, swappable implementation choice, and would force a breaking rename if the backend ever changes — exactly the coupling [ADR-0009](0009-platform-layer-command-runner.md)'s ports-and-adapters discipline already argues against one layer up.
- **Name the top-level model `BootConfiguration`** (the shortest, most generic option). Rejected: it collides with the already-existing, unrelated concept of Boot Manager's own menu configuration (`spec.bootManager.menu` in `ClassroomConfig`, see [docs/CONFIGURATION.md](../CONFIGURATION.md)) — a real ambiguity risk, not a hypothetical one. `FirmwareBootConfiguration` is unambiguous against that term.
- **Name entries `BootOption`** (the UEFI specification's own term). Considered equally valid; not chosen because "boot option" is common shorthand for an unrelated concept (a kernel/bootloader command-line option, as in GRUB) that this model has nothing to do with. `BootEntry` avoids that collision.
- **Fail (raise) on any unrecognized parser line**, rather than permissively skipping it. Rejected: `efibootmgr`'s output format has drifted across versions before and will again; a strict parser would make this adapter brittle against exactly the kind of minor version drift [docs/EFI_ADAPTER.md § Supported `efibootmgr` Versions](../EFI_ADAPTER.md#supported-efibootmgr-versions) already expects to tolerate on a best-effort basis.
- **Merge `LC_ALL`/`LANG` on top of a minimal environment** instead of a full `os.environ` copy. Rejected: `CommandRunner`'s `env` replaces entirely; a minimal environment would drop `PATH` and anything else the child process needs, breaking execution rather than just locale.
- **Wrap `CommandNotFoundError`/`CommandTimeoutError` in adapter-specific equivalents**, for a uniform "every error is `FirmwareBoot*Error`" surface. Rejected as needless indirection: both already are clean, typed, Platform Layer exceptions; wrapping them would add a layer with no new information and would require callers to unwrap to get at the same data `CommandRunner` already exposes.
- **Add execution-metadata fields directly to `FirmwareBootConfiguration`** (duration, timestamps, exit code). Rejected — see Decision point 6; the model represents a machine fact, the pure parser that builds it cannot supply execution facts even if the schema had room for them, and `CommandResult` already carries this data without duplication.
- **Design UEFI boot-entry write/management capability alongside this read-only adapter**, since they're topically related. Rejected: read and write are genuinely different risk profiles (nothing here can damage a machine's boot configuration; a write-capable tool could). Bundling them would also make this ADR's read-only guarantee harder to audit at a glance. A write capability, if ever pursued, gets its own document and its own ADR.
- **A flat `adapters/efi.py` file**, matching `docs/PLATFORM_LAYER.md`'s original sketch literally. Considered acceptable but not chosen: this adapter's internal structure (schema, pure parser, I/O adapter, errors) is developed enough to benefit from separation, and the public import surface is unaffected either way.

## Consequences

- `bcs doctor`/`bcs inventory` gain a path to real UEFI boot-entry visibility, closing a documented gap. The coordination mechanism for this now exists — the Host Discovery Orchestrator ([ADR-0011](0011-host-discovery-orchestrator.md), `Accepted`, implemented) wires this adapter into its `efi` slot at the composition root — but no `bcs` command yet passes `runtime.host_discovery_orchestrator` through to actually consume it (see [docs/EFI_ADAPTER.md § Open Questions](../EFI_ADAPTER.md#open-questions)); accepting this ADR did not itself decide that.
- `FirmwareBootConfiguration`/`BootEntry` join `CommandResult` as models with no `schemaVersion` of their own, since neither is ever expected to be a `bcs` command's own top-level payload — consistent with the precedent [ADR-0009](0009-platform-layer-command-runner.md) already established.
- `bcs.platform.errors.PlatformError` gains its first adapter-level subclasses (`FirmwareBootError` and its two children); any future adapter following this same shape is expected to do the same, keeping a single `except PlatformError` catchable across the whole Platform Layer.
- **The domain-driven-naming rule (point 4) is now project-wide, not EFI-specific.** `lsblk`/`blkid` followed it, superseded by the single, domain-named `storage` adapter rather than shipping as two tool-named modules; `secureboot` followed it too. `mount`/`rsync` remain placeholder tool-names in `docs/PLATFORM_LAYER.md`, not yet individually designed, and are expected to receive domain-appropriate names of their own when they are.
- **The locale policy (point 5) is now project-wide, not EFI-specific.** Every future Platform Layer adapter that executes a Linux command is expected to force `LANG=C`/`LC_ALL=C` the same way, per [docs/PLATFORM_LAYER.md § Locale Policy](../PLATFORM_LAYER.md#locale-policy).
- This ADR's acceptance approves the architecture described in [docs/EFI_ADAPTER.md](../EFI_ADAPTER.md), including its Package Structure/Pydantic Models/Parser Architecture/Adapter Responsibilities/Error Mapping/Testing Strategy sections. **Implemented since:** `models.py`, `parser.py`, `adapter.py`, and `errors.py` — the complete adapter as designed — see [docs/EFI_ADAPTER.md](../EFI_ADAPTER.md)'s own status banner.
- **No effect on already-implemented Platform-001 code.** `bcs.platform.models.CommandResult`, `bcs.platform.errors.PlatformError`/`CommandNotFoundError`/`CommandTimeoutError`/`CommandExecutionError`, `bcs.platform.execution.CommandRunner`/`SubprocessCommandRunner`, and `bcs.context.RuntimeContext.command_runner` are all unchanged — this ADR only adds new, adapter-scoped names underneath them. See [docs/EFI_ADAPTER.md § Backward Compatibility with Platform-001](../EFI_ADAPTER.md#backward-compatibility-with-platform-001) for the explicit, itemized check.
- The read-only guarantee (§ Decision, point 1) remains a hard constraint on any future change to this adapter, not just its initial version — a later contributor proposing to add a write flag to this same module would be reversing this ADR, not extending it, and would need its own ADR to do so.
