# Changelog

All notable changes to Batoi Classroom Suite (BCS) will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). BCS is still overwhelmingly documentation-first — Boot Manager, Builder, and Deploy remain unimplemented — so version numbers largely track the maturity of the *specification*; the `bcs` CLI framework is the first exception with real, tested code.

## [Unreleased]

### Added

- Full documentation set: `ARCHITECTURE.md`, `SPECIFICATION.md`, `ROADMAP.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `AGENTS.md`, `CLAUDE.md`.
- `docs/` tree: architecture deep-dives, component specifications, Architecture Decision Records, coding/Bash/Markdown/naming standards, development and release process docs, contributor guides, and glossary.
- `.github/` issue templates, pull request template, label taxonomy, and Discussions category documentation.
- Placeholder READMEs describing the intended purpose of `boot-manager/`, `builder/`, `deploy/`, `scripts/`, `tools/`, `tests/`, and `assets/`.
- `REVIEW.md`: an independent, critical architecture review of the documentation set, with a prioritized punch list.
- Unified BCS configuration system: `config/schema.yaml` (normative JSON Schema, Kubernetes-style `apiVersion`/`kind`/`metadata`/`spec` envelope), `config/examples/default.yaml` (reference instance), and `docs/CONFIGURATION.md` (full field reference). Resolves the "recipe/manifest" naming ambiguity and the previously-undefined Builder recipe format (`BLD-001`) — see [ADR-0005](docs/decisions/0005-yaml-as-unified-configuration-format.md).

- `docs/CLI.md`: complete design of `bcs`, the unified command-line interface — command tree, global options, exit codes, logging/verbosity, color output, progress reporting, configuration loading, validation flow, and the git-style plugin system. New `CLI-001`–`CLI-014` requirements in `SPECIFICATION.md §2.4`. See [ADR-0006](docs/decisions/0006-bcs-unified-cli-architecture.md).
- `cli/`: the `bcs` CLI framework, implemented in Python (Typer, Rich, Pydantic, PyYAML) per [ADR-0007](docs/decisions/0007-python-for-the-bcs-cli.md) — global options, structured logging, `NO_COLOR`-aware output, the ClassroomConfig loader/override/validation pipeline (Pydantic models mirroring `config/schema.yaml`), git-style plugin dispatch, and the `version`/`doctor`/`validate` commands with placeholder check logic. `build`/`install`/`deploy`/`backup`/`restore`/`update`/`config` are registered as stubs.
- `.pre-commit-config.yaml` and `.github/workflows/ci.yml`: lint (Ruff), type-check (mypy), and test (pytest, Python 3.12/3.13) gates for `cli/`, plus a `bcs` smoke-test job.
- **Host Inventory subsystem** (`bcs.inventory`): immutable, extensible Pydantic models (`HostInventory` and its sections — firmware/Secure Boot, storage, network, identity, OS, CPU, memory, tooling), stdlib-only host collectors, and the `bcs inventory` command. This is BCS's single source of truth describing the current machine, consumed identically by `bcs doctor` (refactored to evaluate pass/fail against these same facts instead of probing the host a second time), and — once implemented — by Boot Manager, Builder, and Deploy, plus a future REST API/Web UI. Carries its own `bcs-inventory/v1alpha1` schema version, independent of `bcs-cli/v1alpha1` and `bcs/v1alpha1`. New `CLI-015` requirement in `SPECIFICATION.md §2.4`. `HostIdentity` narrows (does not resolve) the open `deploy.maintenanceRequests.machineIdentity` question. 209 pytest cases; Ruff, mypy (strict), pre-commit, and GitHub Actions CI all pass.
- `bcs.model_utils`: the `x-`-prefixed-extra-key validator shared by `bcs.config.models` and `bcs.inventory.models`, extracted so both domains can't drift on what counts as a valid extension key.
- `docs/HOST_INVENTORY.md`: a complete design proposal for the Host Inventory subsystem — package structure, Pydantic model diagram, per-class/module responsibilities, dependency graph, sequence diagrams, interaction with the CLI/future REST API/future Web UI, serialization strategy, testing strategy, a current-implementation-status-vs-proposal reconciliation table, and a numbered list of proposed changes requiring approval (a `caveats` field, a checked-in JSON Schema artifact, a golden-file schema-regression test). Supported by [ADR-0008](docs/decisions/0008-host-inventory-ports-and-adapters.md), formalizing the subsystem's ports-and-adapters split, immutability, and JSON-as-canonical-format decisions. **This is documentation only — no `.py`, `pyproject.toml`, or test files were changed**.
- [ADR-0008](docs/decisions/0008-host-inventory-ports-and-adapters.md) accepted: the ports-and-adapters split, immutability, and JSON-as-canonical-format decisions for the Host Inventory subsystem are now in effect. Approving the architecture does not, by itself, approve the individual items in `docs/HOST_INVENTORY.md`'s Proposed Changes Requiring Approval list (the `caveats` field, the checked-in JSON Schema artifact, the golden-file regression test) — those remain open. **Documentation only — no code changed.**
- `EfiSystemPartition` and `UsbStorageDevice` added to the Host Inventory schema, per an amendment to [ADR-0008](docs/decisions/0008-host-inventory-ports-and-adapters.md#amendment-efi-system-partition-and-usb-storage) and new requirement `CLI-016` in `SPECIFICATION.md §2.4`. **Documentation only — no code changed** (`docs/HOST_INVENTORY.md`, `docs/decisions/0008-...`, `SPECIFICATION.md`, `docs/CLI.md`, `ARCHITECTURE.md`).
- Implemented the two new Host Inventory facts: `collect_efi_system_partition()` (probes `/boot/efi` via `/proc/mounts`, resolves the parent disk and filesystem UUID, reports mount/size/free-space state) and `collect_usb_storage()` (enumerates removable, USB-attached block devices via `/sys/block` + sysfs `removable`/device-path heuristics, excluding internal NVMe and non-storage USB peripherals). Both are wired into `collect_host_inventory()`, `bcs inventory`'s text/JSON/YAML rendering, and two new `bcs doctor` checks (`esp`, `usb-storage`). 25 new/updated tests (234 total); Ruff, mypy (strict), and pytest all pass. No Boot Manager, Builder, or Deploy logic was added.
- `docs/PLATFORM_LAYER.md`: a design proposal for the Platform Layer — the `bcs.platform` package that will centralize every OS process execution in `cli/` behind a `CommandRunner` interface, so business/command code never calls `subprocess` directly. Covers package structure, the `CommandRunner` API, the `CommandResult` model, the `PlatformError` exception hierarchy, per-call vs. whole-invocation timeout handling, logging strategy, dependency injection (a proposed `RuntimeContext.command_runner` field), a three-layer testing strategy, and how future adapters (`efibootmgr`, `lsblk`, `blkid`, `mount`, `rsync`) will depend on it. Supported by [ADR-0009](docs/decisions/0009-platform-layer-command-runner.md) (status: `Proposed`) and new requirement `NFR-008` in `SPECIFICATION.md §3` (reusing the existing `NFR-` prefix rather than introducing a new one). **This is documentation only — no `.py`, `pyproject.toml`, or test files were changed**; it is pending explicit approval before any implementation.
- [ADR-0009](docs/decisions/0009-platform-layer-command-runner.md) accepted, with five amendments incorporated directly into the design: the implementation module renamed `runner.py` → `execution.py`; `CommandResult` extended with `command`, `exitCode`, `duration`, `startedAt`, `finishedAt`, `workingDirectory`, and a computed `success` property; "commands are argument lists, `shell=True` is never used" promoted to its own named architectural rule; `docs/standards/coding-standards.md` updated with an explicit, tool-named prohibition (`subprocess.run()`/`subprocess.Popen()`/equivalent outside `bcs.platform`); and the `cli/pyproject.toml` Ruff-scoping plan (`S603`/`S607` narrowed to `bcs.plugins` and `bcs.platform.execution` only) confirmed as approved design. **Documentation only — no code changed** (`docs/PLATFORM_LAYER.md`, `docs/decisions/0009-...`, `docs/standards/coding-standards.md`, `ARCHITECTURE.md`, `docs/CLI.md`); implementation remains gated on a further explicit go-ahead.
- **Platform Layer implemented (Platform-001, Parts 1–3)**: `bcs.platform.models.CommandResult` (frozen, JSON-serializable, computed `success` property, cross-field validators for the `exit_code`/`timed_out` and `started_at`/`finished_at` invariants); `bcs.platform.errors` (`PlatformError`, `CommandNotFoundError`, `CommandTimeoutError`, `CommandExecutionError` — deliberately independent of `bcs.errors.BcsError`); `bcs.platform.execution` (`CommandRunner`, a `runtime_checkable` `Protocol`, and `SubprocessCommandRunner`, wrapping `subprocess.run` with `shell=True` never used, argument lists only, captured stdout/stderr, per-call timeout enforcement). No adapters, no `RuntimeContext` integration, no business logic changed. Ruff, mypy (strict), and pytest all pass with 100% coverage on all three new modules.
- **`CommandRunner` wired into `RuntimeContext` (Platform-001, Part 4)**: `bcs.app.main()` now constructs exactly one `SubprocessCommandRunner` per invocation and injects it as `RuntimeContext.command_runner` — dependency injection only, no existing command's behavior changed, no adapters, no service locator or module-level singleton. `docs/PLATFORM_LAYER.md`'s Dependency Injection section and dependency-graph diagram updated with the ownership/lifecycle detail. 299 pytest cases across `cli/`; Ruff, mypy (strict), and pytest all pass.
- `docs/EFI_ADAPTER.md`: a design proposal for the EFI Adapter — the first **Host Discovery** adapter, a read-only wrapper around `efibootmgr` built on the Platform Layer. Covers package structure (an internal `bcs.platform.adapters.efibootmgr` subpackage: `models.py`/`parser.py`/`adapter.py`/`errors.py`), the `EfiBootConfiguration`/`BootEntry` Pydantic models, a pure-function parser architecture, adapter responsibilities (locale-forced `LC_ALL=C`/`LANG=C` invocation, `check=False`, explicit timeout), error mapping (`EfibootmgrError`/`EfibootmgrUnavailableError`/`EfibootmgrParseError`, extending `PlatformError`), supported `efibootmgr` versions (Ubuntu 24.04 LTS as the pinned reference, best-effort elsewhere), a testing strategy using real captured `efibootmgr -v` output as fixtures, and future extensibility. States a hard, permanent read-only guarantee: never modifies EFI variables, never changes boot order, never installs rEFInd. Supported by [ADR-0010](docs/decisions/0010-efi-adapter-read-only-scope.md) (status: `Proposed`). **This is documentation only — no `.py` files were changed**; it is pending explicit approval before any implementation.
- [ADR-0010](docs/decisions/0010-efi-adapter-read-only-scope.md) accepted, with six amendments incorporated directly into the design: the adapter package renamed `bcs.platform.adapters.efibootmgr` → `.efi` (domain-named, not tied to the current backend tool); models renamed `EfiBootConfiguration` → `FirmwareBootConfiguration` (avoiding a real naming collision with Boot Manager's own `spec.bootManager.menu` "boot configuration") and exceptions `Efibootmgr*Error` → `FirmwareBoot*Error`; the pure parser's independence from the execution layer made an explicit, load-bearing property (`text: str` in, never imports `CommandRunner`/`subprocess`, never knows its input's provenance); a new **project-wide architectural rule** — "packages, adapters, and Pydantic models are named after the business/domain concept, never the implementation technology" — added to `docs/standards/naming-conventions.md` and `docs/standards/coding-standards.md`, and cross-referenced from `ARCHITECTURE.md §5`; the `LANG=C`/`LC_ALL=C` locale requirement promoted from an EFI-specific detail to a Platform Layer-wide rule in `docs/PLATFORM_LAYER.md`; and command-execution metadata evaluated and concluded *not* to belong on `FirmwareBootConfiguration` (with a reusable `CommandMetadata`/envelope concept documented as a deferred, not-yet-necessary idea for future adapters). Explicitly confirmed not to affect any already-implemented Platform-001 public name (`CommandResult`, `PlatformError`, `CommandRunner`/`SubprocessCommandRunner`, `RuntimeContext.command_runner`). **Documentation only — no code changed** (`docs/EFI_ADAPTER.md`, `docs/decisions/0010-...`, `docs/standards/naming-conventions.md`, `docs/standards/coding-standards.md`, `docs/PLATFORM_LAYER.md`, `ARCHITECTURE.md`, `docs/glossary.md`, `docs/architecture/boot-manager.md`); implementation remains gated on a further explicit go-ahead.
- **EFI Adapter fully implemented (Host Discovery)**: `bcs.platform.adapters.efi` — `models.py` (`BootEntry`, `FirmwareBootConfiguration`), `parser.py` (`parse_efibootmgr_output`), `errors.py` (`FirmwareBootError`, `FirmwareBootUnavailableError`, `FirmwareBootParseError`), and `adapter.py` (`read_firmware_boot_configuration`). Frozen, JSON-serializable Pydantic models with full type annotations and docstrings, per `docs/EFI_ADAPTER.md#pydantic-models`. Validates the four-hex-digit boot-number format (`boot_number`, `current_boot_number`, `boot_next`, every `boot_order` entry) and rejects `entries` with duplicate `boot_number` values. The pure-function parser is independent of the execution layer (`text: str` in, never imports `CommandRunner`/`subprocess`). The adapter forces `LANG=C`/`LC_ALL=C`, uses `check=False`, explicit timeout, and maps errors per the ADR-0010 error taxonomy. 79 new tests across `test_platform_adapters_efi_models.py`, `test_platform_adapters_efi_parser.py`, `test_platform_adapters_efi_adapter.py`; Ruff, mypy (strict), and pytest all pass with 100% coverage on the package.
- **Host Discovery fixtures infrastructure** (`cli/tests/fixtures/`): the captured-tool-output corpus for Platform Layer adapter tests, organized by domain (`firmware/` — vendor-split into `generic/`/`dell/`/`hp/`/`lenovo/` per `BM-003`'s known per-OEM variability — plus reserved `storage/`, `secureboot/`, `filesystem/` categories for adapters not yet designed). READMEs document the collection procedure (verbatim stdout of the adapter-specified command), the mandatory `LC_ALL=C LANG=C` locale (matching `docs/PLATFORM_LAYER.md#locale-policy`), format-preserving anonymization rules, the `<tool>_<version>_<platform>_<scenario>.txt` naming convention, and per-category provenance inventory tables. **No fabricated tool output** — the six required EFI scenarios exist as zero-byte placeholder files awaiting capture from real hardware/VMs. Shared, adapter-agnostic loading helpers in `cli/tests/fixture_utils.py` (`fixture_path`/`load_fixture`/`iter_fixtures`/`is_placeholder`; placeholders are skipped by iteration and refused by loading), plus 25 new tests including mechanical corpus-hygiene guards (naming-convention and UTF-8 checks over every corpus file). No parser, no adapter, no business logic, no CLI changes; 362 tests total, Ruff/mypy/pytest all green.

- **Storage Adapter parser implemented (Host Discovery, parser only)**: `bcs.platform.adapters.storage.parser.parse_storage_topology(lsblk_output, blkid_output, findmnt_output)` — a pure function composing `lsblk -J -b`, `blkid -p -o json`, and `findmnt -J` JSON text into an immutable `StorageConfiguration`. Builds the device tree from `lsblk`, annotates partitions with `blkid`'s filesystem/GPT facts, and cross-references `findmnt`'s mount table onto the tree and onto each partition's nested `FilesystemInfo`; never infers a primary/system/boot disk or an installation target. Raises plain `ValueError` for malformed structure (not `StorageParseError`) and lets `pydantic.ValidationError` propagate for model invariants, mirroring `bcs.platform.adapters.efi.parser` exactly — translating to `StorageParseError` remains `adapter.py`'s job, still unimplemented. 30 new tests (`cli/tests/test_platform_adapters_storage_parser.py`) against a synthetic, `tmp_path`-rooted corpus (the real `cli/tests/fixtures/storage/` corpus gained nine zero-byte placeholders — three scenarios × three tools — but no real captures yet) plus an AST-based import-purity check. No `adapter.py`, no `RuntimeContext`/`bcs.inventory` integration, no Host Discovery Orchestrator wiring. Ruff, mypy (strict), and pytest all pass.
- **Storage Adapter fully implemented (Host Discovery, adapter — final part)**: `bcs.platform.adapters.storage.adapter.read_storage_topology(runner, *, timeout_seconds=10.0)` — the orchestration layer, the only module in the package that calls `CommandRunner.run()`. Executes `lsblk -J -b`, `blkid -p -o json`, and `findmnt -J` in sequence (stopping at the first failure), with `LANG=C`/`LC_ALL=C` forced and `check=False` on every call, then hands their stdout to `parse_storage_topology`. Contains no parsing logic. `CommandNotFoundError`/`CommandTimeoutError` propagate unchanged (matching the EFI adapter's own precedent exactly, and consistent with `StorageUnavailableError`'s own "tool present but environment unavailable" docstring); a non-zero exit maps to `StorageUnavailableError` (recognised "unavailable" `stderr`) or `StorageError` (anything else); a parser `ValueError` is wrapped as `StorageParseError`, chained via `from exc`. `docs/STORAGE_ADAPTER.md`'s Error Mapping section is amended accordingly, documenting this as a deliberate deviation from the design doc's earlier, pre-EFI-precedent "tool not found → `StorageUnavailableError`" wording. 27 new tests (`cli/tests/test_platform_adapters_storage_adapter.py`), a `FakeCommandRunner` stand-in keyed by tool name, zero real subprocess execution; `adapter.py` at 100% coverage. This completes the Storage Adapter as designed — `models.py`, `errors.py`, `parser.py`, and `adapter.py` are all implemented. No `RuntimeContext`/`bcs.inventory` integration, no Host Discovery Orchestrator wiring, no Secure Boot/Filesystem adapters. Ruff, mypy (strict), and pytest all pass.
- **Host Discovery Orchestrator — data-holding types implemented (Part 1)**: `bcs.inventory.discovery.models.HostDiscoveryAdapters` (a frozen `dataclass`, one optional `Callable` slot per Discovery domain — `efi`, `storage`, `secure_boot`, `filesystem`, `network`, `cpu`, `memory`, `tpm` — explicit named slots, no dynamic registry, no plugin mechanism, no string-keyed dict) and `bcs.inventory.discovery.models.HostDiscoverySnapshot` (a frozen, `camelCase`-aliased Pydantic model mirroring those same eight domains plus `caveats`). Two corrections made during implementation, both documented in `docs/HOST_DISCOVERY_ORCHESTRATOR.md`: `HostDiscoveryAdapters.network` is typed `Callable[[], list[NetworkInterface]]`, not `tuple[...]`, matching `collectors.collect_network`'s real return type (this project's own architecture review of the design, finding 6); and a `HostDiscoverySnapshot` is hashable only when `network` is empty — any populated `NetworkInterface` breaks it via its own `ip_addresses: list[str]` field, regardless of whether that list is empty, the same limitation `HostInventory` itself already has. [ADR-0011](docs/decisions/0011-host-discovery-orchestrator.md) and `docs/HOST_DISCOVERY_ORCHESTRATOR.md` accepted as part of this change. 18 new tests (`cli/tests/test_inventory_discovery_models.py`); `discovery/models.py` at 100% coverage. No `orchestrator.py`, no composition-root wiring, no `RuntimeContext`/`bcs.inventory` integration, no Secure Boot/Filesystem business logic. Ruff, mypy (strict), and pytest all pass.
- **Host Discovery Orchestrator — coordination logic implemented (Part 2)**: `bcs.inventory.discovery.orchestrator.HostDiscoveryOrchestrator`, constructed with a `HostDiscoveryAdapters` bundle, with one public method, `discover() -> HostDiscoverySnapshot`. Calls every configured (non-`None`) adapter slot exactly once, in the fixed order `efi`/`storage`/`secure_boot`/`filesystem`/`network`/`cpu`/`memory`/`tpm`, via a small `[T]`-generic private helper (`_call_adapter`) rather than a loop over a dict of field names. A `PlatformError` (base class or any subclass) raised by one adapter leaves that domain's field `None`/empty and appends one `"{domain}: {ExceptionType}: {message}"` entry to `caveats`; it never stops the remaining domains. Any other exception propagates unmodified — same instance, no wrapping — and halts the sweep immediately, so domains after the failing one are never called. The `network` slot's `list[NetworkInterface]` result is converted to a `tuple` on the snapshot, and stays `()` (never `None`) whether the slot is unset or fails. Never imports `subprocess` or `bcs.platform.execution.CommandRunner` — verified by an AST-based purity test, not just by reading the code. 17 new tests (`cli/tests/test_inventory_discovery_orchestrator.py`) using lightweight fake adapter callables (a small `_CountingAdapter[T]` plus plain functions for the execution-order test) — no `FakeCommandRunner`, no subprocess mocking of any kind; `discovery/orchestrator.py` at 100% coverage. This completes the orchestrator's own coordination logic as designed. No composition-root wiring, no `RuntimeContext`/`bcs.inventory` integration, no adapter implementation changes. Ruff, mypy (strict), and pytest all pass.
- **Host Discovery Orchestrator — Host Inventory integration implemented (Part 3)**: `bcs.inventory.service.collect_host_inventory()` gains an optional `orchestrator: HostDiscoveryOrchestrator | None = None` parameter. Omitted (the default), behaviour is byte-for-byte identical to before this parameter existed. Given, `orchestrator.discover()` is called exactly once; its `HostDiscoverySnapshot` supplies `network` unconditionally (an empty list is already a valid `HostInventory.network` value) and `cpu`/`memory` whenever the snapshot's own `cpu`/`memory` fields are not `None`, falling back to the same `collectors.collect_cpu()`/`collect_memory()` call otherwise — a refinement beyond `docs/HOST_DISCOVERY_ORCHESTRATOR.md`'s original "satisfied from the snapshot instead" wording, needed because `HostInventory.cpu`/`memory` are *required* fields while the snapshot's are `Optional`, so an unconfigured or `PlatformError`-isolated adapter slot must not be passed through as `None`. Every other `HostInventory` section (`identity`, `firmware`, `operating_system`, `efi_system_partition`, `storage`, `usb_storage`, `tooling`) is unaffected either way. No `try`/`except` wraps `orchestrator.discover()` — any non-`PlatformError` exception (the only kind that can reach here, since `PlatformError` is already isolated inside the orchestrator) propagates unmodified, matching how no existing collector call is wrapped. Per [ADR-0011](docs/decisions/0011-host-discovery-orchestrator.md) Decision point 6, this does **not** add `firmwareBootConfiguration`/`storageTopology`/`secureBoot`/`filesystem`/`tpm`/`caveats` as new `HostInventory` fields — that remains a separate, not-yet-accepted follow-up. 11 tests in `cli/tests/test_inventory_service.py` (2 pre-existing, unchanged, plus 9 new) covering the no-orchestrator/omitted-orchestrator/explicit-`None` cases, snapshot-sourced values overriding collectors, the other seven sections' independence, exactly-once invocation, the `cpu`/`memory` fallback (both the unset-slot and the isolated-`PlatformError` cases), `network`'s no-fallback behaviour, and unexpected-exception propagation; `service.py` at 100% statement and branch coverage. No EFI/Storage adapter, `RuntimeContext`, CLI command, Boot Manager, Builder, or Deploy changes. Ruff, mypy (strict), and pytest all pass; full suite (654 tests) green.
- **Host Discovery Orchestrator — composition-root wiring and `RuntimeContext` integration implemented (Part 4)**: `bcs.app.main()` now builds a `HostDiscoveryAdapters` bundle and a `HostDiscoveryOrchestrator` once per invocation, at the same point `SubprocessCommandRunner` is already built, and injects the orchestrator as a new required `RuntimeContext.host_discovery_orchestrator` field — exactly the treatment `command_runner` received in Platform-001 Part 4. `efi`/`storage` are bound via `functools.partial(read_firmware_boot_configuration, runner=command_runner)`/`functools.partial(read_storage_topology, runner=command_runner)`, sharing the exact same `CommandRunner` instance `RuntimeContext.command_runner` carries; `network`/`cpu`/`memory` are bound directly to `bcs.inventory.collectors`' own already-zero-argument functions, with no `functools.partial` needed; `secure_boot`/`filesystem`/`tpm` stay unset — no `adapter.py` exists for any of them yet (Secure Boot currently has only `models.py`/`parser.py`/`errors.py`), so per the design's own "an unset slot means no adapter wired, not an error" rule, they are simply omitted rather than stubbed. Construction happens exactly once per invocation (never lazily per adapter slot, never a module-level singleton or service locator) — two separate invocations get two independently-constructed orchestrators. `RuntimeContext` exposes the exact instance `bcs.app.main()` built, not a copy. No CLI command's observable behaviour changes: nothing yet passes `runtime.host_discovery_orchestrator` into `collect_host_inventory()` — that remains a separate, later step, consistent with how `command_runner` itself shipped (DI wiring first, collector migration later). 8 new tests (`cli/tests/test_host_discovery_wiring.py`, mirroring `tests/test_command_runner_wiring.py`'s own `monkeypatch`-capturing/counting approach at the `bcs.app` module level) covering exactly-once construction of both the orchestrator and the adapters bundle, fresh reconstruction across separate invocations, the shared-`CommandRunner`-instance property, the exact adapter-slot wiring (including the three omitted slots), and `RuntimeContext`'s correct exposure of the built instance; plus 2 new tests in `cli/tests/test_context.py` (a "Host Discovery Orchestrator Part 4" section mirroring its existing `command_runner` tests) for dataclass-level exposure and identity-preservation of an explicitly-supplied orchestrator. `tests/conftest.py`'s `make_runtime_context` factory gained an optional `host_discovery_orchestrator` parameter, defaulting to an all-`None`-adapters orchestrator, mirroring `command_runner`'s own optional-injection pattern. No new adapters, no business logic, no inventory/CLI command changes, no Boot Manager/Builder/Deploy changes. Ruff, `ruff format --check`, mypy (strict), and pytest all pass; full suite (737 tests) green.
- [docs/SECURE_BOOT_ADAPTER.md](docs/SECURE_BOOT_ADAPTER.md) accepted: the design for the Secure Boot Adapter, the third Host Discovery adapter, wrapping `mokutil --sb-state` — no new ADR required (`docs/SECURE_BOOT_ADAPTER.md#adr-recommendation` concludes every architectural mechanism it uses was already decided by ADR-0008/0009/0010). **Documentation only — no code changed.**
- **Secure Boot Adapter domain models implemented (Host Discovery, models only — Part 1)**: `bcs.platform.adapters.secureboot.models.SecureBootState` (an independently defined `StrEnum` — `ENABLED`/`DISABLED`/`UNSUPPORTED`/`UNKNOWN` — deliberately not imported from `bcs.inventory.models.SecureBootState`, to keep `bcs.platform` from depending upward into `bcs.inventory`) and `.SecureBootStatus` (frozen, JSON-serializable Pydantic model: `state`, `setup_mode: bool | None`, `raw_text`). Named `SecureBootStatus`, not `SecureBootConfiguration`, to avoid colliding with the existing, policy-flavored `spec.security.secureBoot.mode`. No cross-field validation — the model has no collection field for a uniqueness constraint to apply to; validation is limited to intrinsic Pydantic field typing (`state` must be a recognized enum value). Directory named `secureboot` (one word), matching the domain category already reserved in the fixture corpus (`cli/tests/fixtures/secureboot/`) and the sibling `firmware`/`storage`/`filesystem` names. No `parser.py`, `adapter.py`, `errors.py`, `RuntimeContext` integration, or Host Discovery Orchestrator wiring. 18 new tests (`cli/tests/test_platform_adapters_secureboot_models.py`) covering construction, defaults, every enum value, `populate_by_name` aliases, validation (invalid state, extra fields, missing required fields), immutability, equality, hashability, JSON round-tripping, and independence from `bcs.inventory.models.SecureBootState`; `secureboot/models.py` and `secureboot/__init__.py` at 100% coverage. No EFI/Storage adapter, `RuntimeContext`, Host Discovery integration, CLI command, Boot Manager, Builder, or Deploy changes. Ruff, mypy (strict), and pytest all pass; full suite (672 tests) green.
- **Secure Boot Adapter parser implemented (Host Discovery, parser only — Part 2)**: `bcs.platform.adapters.secureboot.parser.parse_secure_boot_status(text: str) -> SecureBootStatus` — a pure function parsing `mokutil --sb-state` output line by line. Recognizes the literal `SecureBoot enabled`/`SecureBoot disabled` and `SetupMode enabled`/`SetupMode disabled` lines (tolerating extra internal whitespace); any other line is silently ignored, and text with no recognized line at all still returns a valid `SecureBootStatus` (`state=UNKNOWN`, `setup_mode=None`) rather than an error. A line starting with the literal `SecureBoot `/`SetupMode ` prefix whose value is neither `enabled` nor `disabled` raises a plain `ValueError` naming the field, the 1-based line number, and the offending line verbatim — mirroring `_raise_malformed`'s shape in `bcs.platform.adapters.efi.parser` exactly. A later duplicate line overwrites an earlier one (the same left-to-right overwrite semantics `FirmwareBootConfiguration`'s own scalar fields already have). `state=UNSUPPORTED` is never produced by this parser — that value is reserved for the not-yet-implemented adapter layer's Error Mapping (a non-zero exit/`stderr` condition, never stdout text). No cross-field validation, matching the model's own lack of a uniqueness constraint. Imports only `re`, `typing.NoReturn`, and its own `secureboot.models` — no `subprocess`, `CommandRunner`, `bcs.platform.adapters.efi`/`storage`, `bcs.inventory`, or `click`, verified by an AST-based purity test. 25 new tests (`cli/tests/test_platform_adapters_secureboot_parser.py`) against a `tmp_path`-rooted synthetic corpus loaded via `fixture_utils.py` (the real `cli/tests/fixtures/secureboot/` corpus stays untouched, still holding only its placeholder README), covering every recognized pattern, whitespace/CRLF tolerance, blank lines, unrecognized-line permissiveness, empty input, duplicate-line overwrite, both malformed-field rejections, and the import-purity check; `secureboot/parser.py` is at 100% statement and branch coverage. No `adapter.py`, `errors.py`, `__init__.py` re-export change, `RuntimeContext` integration, Host Discovery integration, CLI/inventory wiring, Boot Manager, Builder, or Deploy changes. Ruff, mypy (strict), and pytest all pass; full suite (697 tests) green.

- **Secure Boot Adapter error hierarchy implemented (Host Discovery, errors only — Part 3)**: `bcs.platform.adapters.secureboot.errors` defines three exception classes following the same taxonomy as `bcs.platform.adapters.efi.errors` and `bcs.platform.adapters.storage.errors`. `SecureBootError(PlatformError)` carries an optional `result: CommandResult | None` attribute — the `result` field is `None` when the error originates from a caller-supplied message rather than a failed command execution. `SecureBootUnavailableError(SecureBootError)` is raised when the Secure Boot state cannot be determined because the environment cannot answer (tool not found or command fails without parseable output) — it does not mean Secure Boot is disabled, only that it cannot be read. `SecureBootParseError(SecureBootError)` carries a `text: str` attribute naming the unparseable input verbatim; it is raised when `mokutil` output is present but malformed in a way the parser cannot recover from (distinct from the parser's own plain `ValueError` for field-level malformed lines, which is intentionally not wrapped — callers who want to distinguish parse failures from adapter-layer errors should catch `ValueError` separately). All three classes are exported from `secureboot/__init__.py`. No `adapter.py`, `RuntimeContext` integration, Host Discovery Orchestrator wiring, CLI/inventory wiring, Boot Manager, Builder, or Deploy changes. 32 new tests (`cli/tests/test_platform_adapters_secureboot_errors.py`) covering the full inheritance hierarchy, attribute storage, catchable-as-`PlatformError`/`SecureBootError`, and the specific use-case scenarios for each subclass; `secureboot/errors.py` at 100% coverage. Ruff, mypy (strict), and pytest all pass; full suite (729 tests) green.
- **Documentation consistency pass**: corrected factual claims that had fallen out of date relative to the actual state of `cli/` — no design decisions, architecture, or behavior changed. `README.md`/`docs/README.md`: the EFI Adapter's documentation-table entry no longer says "not yet implemented" (it is fully implemented), and gained missing rows for the Storage Adapter, Secure Boot Adapter, and Host Discovery Orchestrator design documents, none of which were previously listed. `docs/PLATFORM_LAYER.md`: the top status banner, package-structure diagram, dependency diagram, and "How Future Adapters Use It" section no longer claim the EFI adapter's `parser.py`/`adapter.py`/`errors.py` are unimplemented, that `lsblk`/`blkid` are undesigned (they were superseded by the accepted, implemented Storage Adapter), or that nothing calls into the Platform Layer yet (`collect_host_inventory()` does, via the Host Discovery Orchestrator). `docs/decisions/0011-host-discovery-orchestrator.md`: the Consequences section now reflects Parts 1–4 as implemented (composition-root wiring and `RuntimeContext.host_discovery_orchestrator` both exist) rather than "not yet made." `docs/guides/getting-started.md`: no longer claims there is no installable software at all — `cli/` is real, tested, installable Python. `docs/processes/development-workflow.md`: no longer claims no CI pipeline exists (`.github/workflows/ci.yml` does, scoped to `cli/`/`config/`); the still-missing Bash/Markdown-specific CI gates are unchanged. `docs/CONFIGURATION.md` and `config/schema.yaml`'s header comment: no longer claim no validator exists — `bcs validate` (`cli/src/bcs/config/validator.py`) does; Builder ingestion and Deploy consumption remain the only genuinely unimplemented pieces. `AGENTS.md`: the Project Orientation summary no longer groups Storage, Secure Boot, and the Host Discovery Orchestrator together as "designed or in progress" — Storage and the Orchestrator are fully implemented, and only Secure Boot's `adapter.py` remains outstanding. `ROADMAP.md`: Phase 0's table gained rows for the Storage Adapter, Secure Boot Adapter, and Host Discovery Orchestrator (ADR-0011), which were previously missing entirely despite being extensively implemented and tested. `cli/src/bcs/inventory/discovery/__init__.py` and `cli/src/bcs/platform/adapters/secureboot/__init__.py`: docstring-only corrections — the former no longer says composition-root wiring/`RuntimeContext` integration don't exist (they do); the latter no longer says `parser.py` doesn't exist (it does; it is just not yet re-exported from this package's own top level). `.github/workflows/ci.yml`: one comment updated for a heading rename in `development-workflow.md`. No Python behavior, public API, test, Boot Manager, Builder, or Deploy changes.
- **Platform Layer consolidation pass**: a second documentation-accuracy sweep across every Platform Layer/Host Discovery document (`docs/PLATFORM_LAYER.md`, `docs/HOST_INVENTORY.md`, `docs/HOST_DISCOVERY_ORCHESTRATOR.md`, `docs/EFI_ADAPTER.md`, `docs/STORAGE_ADAPTER.md`, `docs/SECURE_BOOT_ADAPTER.md`, `docs/FILESYSTEM_ADAPTER.md`) and ADR-0008 through ADR-0011, correcting several "none of this is implemented yet"-style Consequences statements left stale since each ADR's acceptance (ADR-0008's EFI System Partition/USB Storage amendment, ADR-0009, ADR-0010 all now note what has since been implemented), a stale `Proposed` cross-reference to ADR-0011 (now `Accepted`), resolved-but-still-open-looking cross-adapter Open Questions (the `filesystem`-vs-`storage` boundary, the deferred `CommandRunner`-migration question, the Storage Adapter's own deferred `df` question), a hypothetical-ADR-number collision between `SECURE_BOOT_ADAPTER.md` and `FILESYSTEM_ADAPTER.md` (both independently reserved "ADR-0012" for unrelated decisions), and several stale source docstrings (`bcs.platform.__init__`, `bcs.inventory.discovery.models`, `bcs.inventory.discovery.orchestrator`, a comment in `bcs.app`). The `cli/tests/fixtures/` corpus READMEs (root, `secureboot/`, `filesystem/`) were corrected to stop saying "no adapter designed yet" for domains that already had accepted or proposed designs. No design decisions, architecture, code behavior, or public API changed — documentation and comments only. Ruff, mypy (strict), and pytest all pass; full suite (737 tests) unchanged.
- **Secure Boot Adapter fully implemented (Host Discovery, adapter — final part)**: `bcs.platform.adapters.secureboot.adapter.read_secure_boot_status(runner, *, timeout_seconds=5.0)` — the orchestration layer, the only module in the package that calls `CommandRunner.run()`, following the identical shape and style already established by `bcs.platform.adapters.efi.adapter`. Executes `["mokutil", "--sb-state"]` with `LANG=C`/`LC_ALL=C` forced and `check=False`, then hands `result.stdout` to `parser.parse_secure_boot_status`. Contains no parsing logic. `CommandNotFoundError`/`CommandTimeoutError` propagate unchanged. A non-zero exit maps to `SecureBootUnavailableError` (recognised "environment cannot provide Secure Boot data" `stderr`) or `SecureBootError` (anything else); a parser `ValueError` (a malformed field) is wrapped as `SecureBootParseError`, chained via `from exc`. A zero-exit result whose parsed `SecureBootStatus` has `state == UNKNOWN` **and** `setup_mode is None` — meaning the output contained no recognized line at all — is also rejected as `SecureBootParseError`, per `docs/SECURE_BOOT_ADAPTER.md#adapter-responsibilities` point 4; a result with only a recognized `SetupMode` line (no `SecureBoot` line) is *not* rejected, since at least one line was recognized. `secureboot/__init__.py` now re-exports `parse_secure_boot_status` and `read_secure_boot_status` alongside the previously-exported models/errors, completing the package's public surface to match the EFI/Storage adapters' own. 22 new tests (`cli/tests/test_platform_adapters_secureboot_adapter.py`), a `FakeCommandRunner` stand-in mirroring `test_platform_adapters_efi_adapter.py`'s own style, zero real subprocess execution; `adapter.py` at 100% coverage. This completes the Secure Boot Adapter as designed — `models.py`, `errors.py`, `parser.py`, and `adapter.py` are all implemented, reaching feature parity with the EFI and Storage adapters. The `cli/tests/fixtures/secureboot/` corpus gained its required command line, layout, and five zero-byte placeholder scenario fixtures (`enabled`, `disabled`, `setup-mode`, `no-setup-mode-line`, `unavailable-stderr`) per `docs/SECURE_BOOT_ADAPTER.md § Fixtures Strategy` — no real capture yet. `docs/SECURE_BOOT_ADAPTER.md`, `docs/PLATFORM_LAYER.md`, `README.md`, `docs/README.md`, `AGENTS.md`, and `ROADMAP.md` updated to reflect the adapter as fully implemented rather than partial. No `RuntimeContext`/Host Discovery Orchestrator wiring (the `secure_boot` slot stays unset in `bcs.app.main()`), no CLI/inventory wiring, no Boot Manager, Builder, or Deploy changes — those remain separate, later steps. Ruff, `ruff format --check`, mypy (strict), and pytest all pass; full suite (769 tests) green.
- **Secure Boot Adapter wired into the Host Discovery composition root**: `HostDiscoveryAdapters.secure_boot`/`HostDiscoverySnapshot.secure_boot` (`cli/src/bcs/inventory/discovery/models.py`) narrowed from the generic placeholder type `Callable[[], object] | None`/`object | None` to the concrete `Callable[[], SecureBootStatus] | None`/`SecureBootStatus | None`, matching the treatment `efi`/`storage` already had — exactly the step `docs/SECURE_BOOT_ADAPTER.md § Future Extensibility` and `docs/HOST_DISCOVERY_ORCHESTRATOR.md § Future Extensibility` both already anticipated once the adapter reached acceptance. `bcs.app.main()`'s composition root now binds `secure_boot=functools.partial(read_secure_boot_status, runner=command_runner)`, sharing the exact same `CommandRunner` instance the `efi`/`storage` slots and `RuntimeContext.command_runner` already share. No change to `HostDiscoveryOrchestrator.discover()`'s own coordination logic — it already called whatever the `secure_boot` slot held; a real `PlatformError` raised by this adapter (e.g. `mokutil` not found) now produces a `caveats` entry exactly like `efi`/`storage` already do, instead of being silently absent as an unset slot. `bcs.inventory.service.collect_host_inventory(orchestrator)` receives Secure Boot data through the existing discovery flow unchanged — `orchestrator.discover()`'s snapshot now genuinely computes `secure_boot`, exactly mirroring how `firmware_boot_configuration`/`storage_topology` already flow through today; per [ADR-0011](docs/decisions/0011-host-discovery-orchestrator.md) Decision point 6, this does **not** add a new `HostInventory` field for it — folding Discovery-domain facts into `HostInventory`'s own schema remains a separate, not-yet-approved ADR-0008 amendment, so `collect_host_inventory()` itself is unmodified. `filesystem`/`tpm` remain unset (no `adapter.py` exists for either), and `bcs doctor`/`bcs inventory` still do not pass `runtime.host_discovery_orchestrator` through — both out of scope for this change. Tests updated: `cli/tests/test_inventory_discovery_models.py` (a real `SecureBootStatus` replaces the generic `lambda: object()` fake for the `secure_boot` slot; the former combined opaque-values test split into a `filesystem`/`tpm`-only test plus a new typed `test_snapshot_accepts_secure_boot_status`), `cli/tests/test_inventory_discovery_orchestrator.py` (fake adapters now return real `SecureBootStatus` instances instead of placeholder strings/`None`), and `cli/tests/test_host_discovery_wiring.py` (the shared-`CommandRunner` test and the adapters-bundle-wiring test both extended to cover `secure_boot`). 1 new test (`test_snapshot_accepts_secure_boot_status`); full suite (770 tests) green. `docs/HOST_DISCOVERY_ORCHESTRATOR.md` (dependency/component diagrams, the `HostDiscoveryAdapters`/`HostDiscoverySnapshot` field tables, the Dependency Injection Strategy and Error Propagation sections, Future Extensibility) and `docs/SECURE_BOOT_ADAPTER.md` (status banner, Future Extensibility, Backward Compatibility, Open Questions, ADR Recommendation citing ADR-0011, Related Documents) updated to match. `AGENTS.md`/`docs/PLATFORM_LAYER.md` no longer say the `secure_boot` slot is unwired. No adapter internals changed, no new abstractions, no ADR required (ADR-0011 already specifies this exact composition-root wiring mechanism for any accepted adapter). Ruff, `ruff format --check`, mypy (strict), and pytest all pass.
- **Host Discovery pipeline validated end to end (no code defects found)**: a full review of the complete execution path — `bcs.app.main()`'s composition root, `RuntimeContext` construction, dependency injection, `HostDiscoveryOrchestrator` usage, `HostDiscoverySnapshot` creation, per-adapter invocation counts, failure propagation, and `caveats` generation — against `docs/HOST_DISCOVERY_ORCHESTRATOR.md` and [ADR-0011](docs/decisions/0011-host-discovery-orchestrator.md)'s own specification found no integration bug; the gap was test coverage, not behavior. Added `cli/tests/test_host_discovery_pipeline.py` (7 new tests): a shared, multi-tool `FakeCommandRunner` (keyed by `command[0]`, mirroring `test_platform_adapters_storage_adapter.py`'s own fake) binds the three *real, currently-implemented* adapters (`read_firmware_boot_configuration`/`read_storage_topology`/`read_secure_boot_status` — not lightweight fake callables) via the exact `functools.partial` shape `bcs.app.main()` uses, alongside the real, unfaked `network`/`cpu`/`memory` collectors. Verifies: every tool-based adapter is invoked exactly once per `discover()` call (5 distinct tools: `efibootmgr`, `lsblk`, `blkid`, `findmnt`, `mokutil`); the locale-forced environment reaches every adapter; a `PlatformError` from one real adapter (`CommandNotFoundError`, and separately a recognizable "unavailable" `stderr` mapping to that adapter's own `*UnavailableError` subclass) isolates into exactly one `caveats` entry in the exact `"{domain}: {ExceptionType}: {message}"` format ADR-0011 specifies, while every other domain still succeeds; multiple independent failures each produce their own caveat, in field order; calling `discover()` twice re-invokes every real adapter a second time. One test drives the pipeline through the real, unmodified composition root itself — patching only the sanctioned `SubprocessCommandRunner` seam, invoking a real CLI command via `CliRunner`, capturing the exact `HostDiscoveryOrchestrator` instance `bcs.app.main()` built, and calling `discover()` on it directly — proving the assembled pipeline works end to end, not just piece by piece. `docs/HOST_DISCOVERY_ORCHESTRATOR.md § Testing Strategy` gained a row documenting this coverage. No adapter internals, interfaces, or abstractions changed; full suite (777 tests) green. Ruff, `ruff format --check`, mypy (strict), and pytest all pass.
- `docs/IMPLEMENTATION_STATUS.md`: a new, generated-from-repository-state implementation status dashboard answering "what is implemented today" in one page — overall project status, an Architecture Components table (Configuration through Deploy), an ADR Status table (all 11 ADRs), a Platform Adapter Matrix (EFI/Storage/Secure Boot/Filesystem), Host Discovery status and current limitations, a Phase Progress count against `ROADMAP.md`, current test/coverage/lint/CI status (777 passing tests, 96% coverage, 100% on every Platform Layer/Host Discovery module), an Outstanding Work list grouped by priority and linked to its owning design document or ADR rather than restated, a Mermaid diagram of the implemented-only architecture, and a reading guide. Links added from `README.md` (Project Status section and Documentation table) and `docs/README.md` (Contents table). **Documentation only — no architecture, ADR, or code changed.**
- `docs/PATTERNS.md`: a new canonical implementation methodology for Platform Layer adapters, extracting into one document the workflow that previously existed only implicitly across the EFI, Storage, and Secure Boot adapters' own design documents and `CHANGELOG.md` entries. Covers: its own purpose and relationship to `AGENTS.md`/`PLATFORM_LAYER.md`; the complete 15-stage adapter lifecycle (design proposal through final quality gates, including a note on why models/errors/parser have no dependencies on each other despite the historical Models→Parser→Errors→Adapter build order); a Definition of Done checklist; the testing strategy extracted from all three implemented adapters (model/parser/adapter/protocol-conformance/AST-purity tests, malformed-input handling, coverage expectations, and the two kinds of integration test established by `cli/tests/test_host_discovery_pipeline.py`); the canonical adapter design-document section order (drawn from `SECURE_BOOT_ADAPTER.md`/`FILESYSTEM_ADAPTER.md`, the two most refined examples) and every other document a completed adapter touches; the architecture rules every adapter must follow (adapters execute commands, parsers never execute commands or infer facts, models only represent facts, adapters translate exceptions, the orchestrator isolates only `PlatformError`, the composition root owns DI, the full-`os.environ`-copy locale policy, domain-driven naming, no upward dependency from `bcs.platform` into `bcs.inventory`, proportionality per `REVIEW.md §7`); a list of real, previously-made mistakes with links to their actual fixes (missing `__init__.py` exports, a missing `CHANGELOG.md` entry, stale "not yet implemented" docstrings, the ADR-0011 status and "ADR-0012" reservation collisions, the two-key-`env`-dict locale bug, and others); and a concise, mechanical 17-step checklist for a future implementer (human or AI agent) to follow for the next adapter (Filesystem, Network, CPU, Memory, TPM, or any other). Links added from `README.md` and `docs/README.md` (Documentation/Contents tables) and `docs/IMPLEMENTATION_STATUS.md` (Reading Guide and the Outstanding Work list's Network/CPU/Memory/TPM item). **Documentation only — no architecture, ADR, or code changed.**
- **Filesystem Adapter domain models implemented (Host Discovery, models only — Part 1)**: `docs/FILESYSTEM_ADAPTER.md` accepted (status: `Accepted`, up from `Proposed`) and its first part implemented, following the exact process in `docs/PATTERNS.md`. `bcs.platform.adapters.filesystem.models.FilesystemUsage` (per-mount usage: `source`, `target`, `fs_type`, `size_bytes`/`used_bytes`/`available_bytes` as `int` (`ge=0`), `inodes_total`/`inodes_used`/`inodes_available` as `int | None` (`ge=0` when present, `None` meaning `df`'s own `-` "not supported for this filesystem type" token), `raw_line`) and `FilesystemUsageReport` (`filesystems: tuple[FilesystemUsage, ...]` defaulting to `()`, `raw_text`, `raw_stderr` defaulting to `""`). Both frozen, `extra="forbid"`, `populate_by_name=True`, `camelCase` JSON aliases, no `schemaVersion` (never a `bcs` command's own top-level payload) — the identical shape every sibling adapter's own models use. Validation is intentionally narrow: only the `ge=0` bounds on every byte/inode field; no `used_bytes + available_bytes == size_bytes` check (reserved filesystem blocks routinely make that arithmetic not hold for healthy filesystems) and, notably, **no duplicate-`target` rejection** — `FilesystemUsageReport` accepts two `FilesystemUsage` entries sharing the same `target`, since mount stacking (overmounting) is a real, legitimate machine state this adapter's "expose facts, never hide them" mandate requires preserving rather than rejecting or silently deduplicating. No interpretation, no heuristics, no derived fields (no percentage-used, no "is this low on space" judgment) anywhere on either model, per `docs/FILESYSTEM_ADAPTER.md#scope-guarantee`. `__init__.py` re-exports both models; `parser.py`/`errors.py`/`adapter.py` do not exist yet. 42 new tests (`cli/tests/test_platform_adapters_filesystem_models.py`) covering construction, defaults (including the `raw_stderr=""`/`filesystems=()` defaults), every alias spelling, the `ge=0` validators (including that zero itself is accepted and that the "used ≤ size" and "used + available == size" inequalities are deliberately *not* enforced), forbidding extra fields, every mandatory field's absence raising, immutability (including nested `FilesystemUsage` entries), equality, hashability (`FilesystemUsageReport` is unconditionally hashable — unlike `HostDiscoverySnapshot`, nothing here is a plain `list`), JSON round-tripping (including `None` inode fields and duplicate-`target` entries), and the explicit, dedicated duplicate-`target`-is-accepted-not-rejected test called out in the design document's own Testing Strategy. `filesystem/models.py` and `filesystem/__init__.py` both at 100% statement and branch coverage; full suite (819 tests) green. `docs/FILESYSTEM_ADAPTER.md` updated: status banner, Package Structure (`__init__.py`/`models.py` marked implemented), Domain Models section, and the Testing Strategy table's models row. No parser, no adapter, no errors module, no `RuntimeContext`/composition-root wiring, no Host Discovery Orchestrator wiring, no CLI/inventory wiring, no Boot Manager, Builder, or Deploy changes. Ruff, `ruff format --check`, mypy (strict), and pytest all pass.

### Added

- **Fixture corpus strategy documented for all four Platform Adapters**: `cli/tests/fixtures/filesystem/README.md` rewritten from a "Deliberately empty" placeholder to a full fixture strategy document matching the accepted `docs/FILESYSTEM_ADAPTER.md` design — exact capture command (`df --output=source,fstype,itotal,iused,iavail,size,used,avail,target -B1 -a`), flat layout (no vendor subdirectories, mirroring `secureboot/`), naming convention (`df_<version>_ubuntu-24.04_<scenario>.txt`), and an inventory table with five required scenarios (typical-nvme-laptop, vfat-reports-no-inodes, mount-point-with-space, duplicate-target, and the unavailable-stderr pair). `cli/tests/fixtures/README.md`'s domain table updated to reflect the Filesystem Adapter's accepted status. The existing `firmware/`, `storage/`, and `secureboot/` fixture READMEs were verified and remain correct as-is. **Documentation only — no code, no placeholder files, and no adapter implementation changed.**

### Changed

- `docs/standards/naming-conventions.md`: configuration keys are `camelCase` (Kubernetes/Compose convention), superseding earlier `snake_case` guidance that predated the configuration format being designed; registered the new `CLI-NNN` requirement ID prefix.
- `docs/glossary.md`: "Recipe" now points to the concrete `ClassroomConfig` format instead of describing an undefined artifact; "manifest" is retired as a synonym.
- `ARCHITECTURE.md`: added `§8 Operator Interface`, describing `bcs` as the single entry point into all three components.
- `docs/CLI.md`: `Implementation Notes` now describes the actual Python layout under `cli/`, superseding the originally-planned Bash layout.
- `AGENTS.md`, `docs/repository-organization.md`: updated to reflect `cli/` as an implemented, tested exception to the documentation-only phase.

## [0.1.0] - 2026-07-06

### Added

- Initial project structure: `boot-manager/`, `builder/`, `deploy/`, `assets/`, `docs/`, `scripts/`, `tests/`, `tools/`.
- MIT `LICENSE`.
- Initial `README.md` describing the project's three components.

[Unreleased]: https://github.com/nino79/batoi-classroom-suite/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/nino79/batoi-classroom-suite/releases/tag/v0.1.0
